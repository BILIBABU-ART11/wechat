# 院院通稳定性与安全优化任务

- [x] 增加失败优先测试，覆盖 AppID、Token、远程状态并发/恢复、权限、批次幂等、订阅授权和同步边界。
  - 验收：新增测试在旧实现上能够暴露问题，并在完成实现后全部通过。
- [x] 将远程 JSON 状态升级为 v2，并提供原子业务接口和备份恢复。
  - 验收：v1 数据可迁移；并发写不丢失；主文件损坏可从 `.bak` 恢复；文件权限为 600。
- [x] 改造云托管认证、订阅、导入和提醒流程。
  - 验收：Token 不携带 openid/院院通 ID；旧绑定不可复活；提醒接口使用导入密钥；批次重复触发不重复发送；飞书 webhook 不再存在。
- [x] 改造 Linux 同步脚本和 systemd 定时部署文件。
  - 验收：完整数据只写远程状态；云托管仅接收 batch_id；请求有超时/重试；分页不会死循环；北京时间 09:20 和 17:20 运行且不会重叠。
- [x] 更新小程序订阅交互、AppID、环境示例和部署文档。
  - 验收：AppID 正确；订阅使用 request_id 和微信原始结果；部署配置可直接照文档完成。
- [x] 完成全量测试、静态检查、安全扫描、Git 提交和推送。
  - 验收：测试、语法、JSON、结构、npm audit 和 git diff 检查全部通过，远端 master 包含提交。
